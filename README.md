# Rishav Kumar Portfolio

React + Tailwind portfolio inspired by `luffytaro.me`, rebuilt with Rishav Kumar's content.

## Run locally

```powershell
cmd /c npm install
cmd /c npm run dev
```

## Build

```powershell
cmd /c npm run build
```
